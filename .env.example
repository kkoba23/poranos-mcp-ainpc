# PyPI publish credentials (used by twine upload)
# Real values go in .env (which is gitignored). Never commit .env.
# Get token at https://pypi.org/manage/account/token/
TWINE_USERNAME=__token__
TWINE_PASSWORD=pypi-CHANGE-ME-TO-YOUR-TOKEN
